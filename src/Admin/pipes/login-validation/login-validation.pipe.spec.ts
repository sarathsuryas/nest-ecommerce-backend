import { LoginValidationPipe } from './login-validation.pipe';

describe('LoginValidationPipe', () => {
  it('should be defined', () => {
    expect(new LoginValidationPipe()).toBeDefined();
  });
});
