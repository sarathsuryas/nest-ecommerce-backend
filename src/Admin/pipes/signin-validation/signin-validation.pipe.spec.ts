import { SigninValidationPipe } from './signin-validation.pipe';

describe('SigninValidationPipe', () => {
  it('should be defined', () => {
    expect(new SigninValidationPipe()).toBeDefined();
  });
});
